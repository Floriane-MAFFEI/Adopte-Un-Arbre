<form method="post" action="{{ path('app_back_specie_delete', {'id': specie.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ specie.id) }}">
    <button class="btn">Delete</button>
</form>
