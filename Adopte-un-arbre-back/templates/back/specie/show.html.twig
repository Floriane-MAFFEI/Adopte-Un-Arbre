{% extends 'base.html.twig' %}

{% block title %}Specie{% endblock %}

{% block body %}
    <h1>Specie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ specie.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ specie.name }}</td>
            </tr>
            <tr>
                <th>Scientific_name</th>
                <td>{{ specie.scientificName }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ specie.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_back_specie_index') }}">back to list</a>

    <a href="{{ path('app_back_specie_edit', {'id': specie.id}) }}">edit</a>

    {{ include('back/specie/_delete_form.html.twig') }}
{% endblock %}
